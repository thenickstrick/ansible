---
- hosts: all
  become: true
  pre_tasks:

  - name: install updates for Fedora
    ansible.builtin.dnf: 
      update_only: yes
      update_cache: yes
    when: ansible_distribution == "Fedora"

  - name: install updates for Ubuntu
    ansible.builtin.apt:
      upgrade: dist
      update_cache: yes
    when: ansible_distribution == "Ubuntu"

- hosts: us
  become: true
  tasks:

    - name: uninstall apache from webservers
      ansible.builtin.apt:
        name:
          - apache2
          - libapache2-mod-php
        state: absent
      when: ansible_distribution == "Ubuntu"

- hosts: fs
  become: true
  tasks:
    - name: uninstall mariadb package from db servers
      ansible.builtin.dnf:
        name: mariadb
        state: absent
      when: ansible_distribution == "Fedora"

